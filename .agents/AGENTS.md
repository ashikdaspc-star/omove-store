# Workspace Rules for Omove Store

- **Git & GitHub Directives**: Do NOT push code to GitHub automatically after making local changes. Only execute `git push` to GitHub when the USER explicitly requests an update/push to GitHub.
